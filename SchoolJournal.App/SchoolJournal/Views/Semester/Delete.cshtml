@model SchoolJournal.Models.Semester

@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/_Layout.cshtml";
}

<h2>DELETE</h2>
<h3>Are you sure you want to delete this information?</h3>

<div>
    <h4></h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>@Html.DisplayNameFor(t => t.SemesterID)</dt>
        <dd>@Html.DisplayFor(t => t.SemesterID)</dd>

        <dt>@Html.DisplayNameFor(t => t.SemesterNumber)</dt>
        <dd>@Html.DisplayFor(t => t.SemesterNumber)</dd>

        <dt>@Html.DisplayNameFor(t => t.SchoolYear)</dt>
        <dd>@Html.DisplayFor(t => t.SchoolYear)</dd>
    </dl>
    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()
        <div class="form-group">
            <input type="submit" value="Delete" class="btn btn-primary" />
            @Html.ActionLink("Cancel", "Index", "Semester")
        </div>
    }
</div>