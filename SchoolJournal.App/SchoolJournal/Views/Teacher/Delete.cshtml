@model SchoolJournal.Models.Teacher

<h2>DELETE</h2>
<h3>Are you sure you want to delete this information?</h3>

<div>
    <h4></h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>@Html.DisplayNameFor(t => t.TeacherID)</dt>
        <dd>@Html.DisplayFor(t => t.TeacherID)</dd>

        <dt>@Html.DisplayNameFor(t => t.TeacherName)</dt>
        <dd>@Html.DisplayFor(t => t.TeacherName)</dd>

        <dt>@Html.DisplayNameFor(t => t.TeacherEmail)</dt>
        <dd>@Html.DisplayFor(t => t.TeacherEmail)</dd>

        <dt>@Html.DisplayNameFor(t => t.TeacherPhone)</dt>
        <dd>@Html.DisplayFor(t => t.TeacherPhone)</dd>
    </dl>
    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()
        <div class="form-group">
            <input type="submit" value="Delete" class="btn btn-primary" />
            @Html.ActionLink("Cancel", "Index", "Teacher")
        </div>
    }
</div>
