@model SchoolJournalModels.Homework

<div>
    <h2>UPDATE</h2>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()
        @Html.HiddenFor(h => h.HomeworkID)
        <div class="form-group">
            @Html.LabelFor(h => h.Students.StudentName)
            @Html.DropDownListFor(h => h.StudentID, new SelectList(Model.StudentsList, "Value", "Text"))
        </div>
        <div class="form-group">
            @Html.LabelFor(h => h.Courses.CourseName)
            @Html.DropDownListFor(h => h.CourseID, new SelectList(Model.CoursesList, "Value", "Text"))
        </div>
        <div class="form-group">
            @Html.LabelFor(h => h.DateOfHomework)
            @Html.TextBoxFor(h => h.DateOfHomework, null, new { @class = "form-control" })
            @Html.ValidationMessageFor(h => h.DateOfHomework, "", new { @class = "text-danger" })
        </div>
        <div class="form-group">
            @Html.LabelFor(h => h.DueDate)
            @Html.TextBoxFor(h => h.DueDate, null, new { @class = "form-control" })
            @Html.ValidationMessageFor(h => h.DueDate, "", new { @class = "text-danger" })
        </div>
        <div class="form-group">
            @Html.LabelFor(h => h.Details)
            @Html.TextBoxFor(h => h.Details, null, new { @class = "form-control" })
            @Html.ValidationMessageFor(h => h.Details, "", new { @class = "text-danger" })
        </div>
        <div class="form-group">
            @Html.LabelFor(h => h.HomeworkStatus)
            @Html.TextBoxFor(h => h.HomeworkStatus, null, new { @class = "form-control" })
            @Html.ValidationMessageFor(h => h.HomeworkStatus, "", new { @class = "text-danger" })
        </div>

        <input type="submit" value="Save" class="btn btn-primary" />
        @Html.ActionLink("Cancel", "Index", "Homework");
    }
</div>